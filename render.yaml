services:
  - name: chrome-remote-desktop
    env: docker
    buildCommand: docker build -t chrome-remote-desktop .
    startCommand: docker run -e CHROME_REMOTE_DESKTOP_PIN=123456 chrome-remote-desktop
